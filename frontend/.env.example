# Optional: GraphQL API URI (used for all GraphQL requests and refresh).
# Default: /graphql (Vite dev server proxies to backend; production may need full URL).
# VITE_GRAPHQL_URI=/graphql
# VITE_GRAPHQL_URI=https://your-api.example.com/graphql
